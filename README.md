# node-server-typescript-webpack
node web app with typescript and webpack eslint preconfigured

# clone the repo and run `npm install or yarn` then 
# after installation done run `npm run dev or yarn dev`
